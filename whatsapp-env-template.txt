# WhatsApp Bot Configuration for MediQ Hospital
# Copy these variables to your .env file

# WhatsApp Cloud API Configuration (Meta Business)
# Get these from https://developers.facebook.com/
WHATSAPP_ACCESS_TOKEN=your_access_token_here
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id_here
WHATSAPP_WEBHOOK_URL=https://yourdomain.com/api/whatsapp/webhook
WHATSAPP_VERIFY_TOKEN=your_secure_verify_token_here

# Optional: Test mode (logs messages instead of sending)
WHATSAPP_TEST_MODE=false

# Your existing environment variables
# (Keep all your existing .env variables and add the WhatsApp ones above)

# Database
MONGODB_URI=mongodb://localhost:27017/mediq

# JWT
JWT_SECRET=your_jwt_secret_here

# Email (for notifications)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# SMS (for notifications)
TWILIO_ACCOUNT_SID=your_twilio_sid
TWILIO_AUTH_TOKEN=your_twilio_token
TWILIO_PHONE_NUMBER=your_twilio_number

# Server
PORT=5000
NODE_ENV=development
